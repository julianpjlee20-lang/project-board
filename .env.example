# Database Configuration
DATABASE_URL=postgresql://username:password@localhost:5432/project_board

# Auth.js v5
AUTH_SECRET=          # openssl rand -base64 32 產生
AUTH_TRUST_HOST=true

# Auth.js Provider（與舊變數共存，優先使用 AUTH_ 前綴）
AUTH_DISCORD_ID=      # 同 DISCORD_CLIENT_ID
AUTH_DISCORD_SECRET=  # 同 DISCORD_CLIENT_SECRET
AUTH_LINE_ID=         # 同 LINE_LOGIN_CHANNEL_ID
AUTH_LINE_SECRET=     # 同 LINE_LOGIN_CHANNEL_SECRET

# Discord OAuth（舊版，保留相容）
DISCORD_CLIENT_ID=your_discord_client_id_here
DISCORD_CLIENT_SECRET=your_discord_client_secret_here
DISCORD_REDIRECT_URI=http://localhost:3000/api/auth/discord/callback

# Application
NODE_ENV=development
PORT=3000

# 環境提醒 Banner（production / testing / 不設定則不顯示）
NEXT_PUBLIC_DB_ENV=

# LINE Login（舊版，保留相容）
LINE_LOGIN_CHANNEL_ID=your_line_login_channel_id
LINE_LOGIN_CHANNEL_SECRET=your_line_login_channel_secret
LINE_LOGIN_REDIRECT_URI=http://localhost:3000/api/auth/line/callback

# LINE Messaging API
LINE_MESSAGING_CHANNEL_ACCESS_TOKEN=your_line_messaging_channel_access_token

# Discord Webhook (通知用)
DISCORD_WEBHOOK_URL=your_discord_webhook_url
